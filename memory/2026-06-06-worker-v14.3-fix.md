# Worker v14.3 全面修复 — 2026-06-06 09:44

## 目标
修复 Dr. Frontie 报告的所有 API 和前端问题，达到满意后请她验收。

## 修复内容

### API 层（Worker v14.3）
1. ✅ `GET /messages/{thread_id}` — 按线程筛选消息（v14.1 新增）
2. ✅ `GET /agents` — Agent 列表（v14.1 新增）
3. ✅ `POST /message/{id}/like` — 点赞功能（v14.3 修复 ID 提取 bug，`/message/` 是 9 字符不是 10）
4. ✅ `POST /agent/register` — 注册（v14.0 已修复）
5. ✅ `POST /agent/auth` — 认证（v14.0 已修复）
6. ✅ `GET /messages` 分页参数 — 新增 limit/offset（v14.3）

### 前端层
7. ✅ Community 旧端点 — AgentMessageBoard.tsx 已用 `/messages`
8. ✅ 语言切换器 — LanguageSwitcher.tsx 已集成到三个 Navbar
9. ✅ Digital Tribunal 中文混入英文 — 已移除所有中文 subtitle（GitHub commit 8237937）
10. ✅ API 注册页文案 — 已改为 "Apply for Access"

## 关键 bug 修复
- `/message/{id}/like` 路由中 `path.substring(10)` 应为 `path.substring(9)`（`/message/` 是 9 字符），导致消息 ID 首字符被截断（`msg:` → `sg:`），点赞永远找不到消息

## 部署
- Worker v14.3 部署成功（Cloudflare API）
- 前端代码通过 GitHub Contents API 推送（git push HTTPS 超时）
- 构建 0 错误
